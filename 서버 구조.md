# 서버 구조

## 1. ⚙️ backend/ (백엔드 서버)

FastAPI를 기반으로 한 서버 API, 데이터베이스, AI 분석 엔진 및 보안 로직이 포함된 공간입니다.

- **`backend/requirements.txt`**: 서버 실행에 필요한 기본 패키지 목록
- **`backend/requirements-dev.txt`**: 테스트 및 개발 도구 패키지 목록
- **`backend/pytest.ini`**: 백엔드 테스트 설정 파일
- **`backend/app/`**: FastAPI 기반 핵심 서버 코드 디렉토리
  - **`main.py`**: FastAPI 앱 초기화, 라우트, 미들웨어, CORS, 인증, 세션, 업로드 처리 등 전체 API의 진입점
  - **`analysis_service.py`**: 업로드된 모델과 영상을 실제로 분석하는 코드 (YOLO 모델 로딩, OpenCV 영상 처리, 결과 영상 및 통계 생성)
  - **`backup_restore.py`**: SQLite DB와 업로드 파일을 백업 및 복원하는 스크립트
  - **`database.py`**: 데이터베이스 연결, SQLAlchemy 설정 및 스토리지 경로 정의
  - **`models.py`**: SQLAlchemy ORM 모델 정의 (사용자, 세션, 분석 기록, 게시글, 첨부파일 등 데이터 구조)
  - **`oauth.py`**: OAuth 로그인 처리 및 외부 프로바이더 연동 로직
  - **`schemas.py`**: FastAPI 요청/응답 데이터 검증을 위한 Pydantic 스키마
  - **`security.py`**: 비밀번호 해시, 세션 토큰 생성 및 검증 등 인증 관련 로직
  - **`storage_security.py`**: 파일 업로드/저장/삭제 안전 검사 (디스크 용량, 경로 안전성, 파일 확장자 검증 등)

## 2. 💻 frontend/ (프론트엔드 웹 앱)

Next.js 기반으로 사용자가 직접 보게 되는 웹 UI, 페이지, 컴포넌트, 그리고 API 연동 로직이 포함된 공간입니다.

- **`frontend/package.json`**: 프론트엔드 실행 명령과 의존성 패키지 목록
- **`frontend/next.config.ts`**: Next.js 설정 파일
- **`frontend/tsconfig.json`**: TypeScript 설정 파일
- **`frontend/next-env.d.ts`**: Next.js 타입 선언 파일
- **`frontend/app/`**: 페이지 라우트와 레이아웃
  - **`page.tsx`**: 메인 라우트 (공개 홈 또는 인증 페이지로 리다이렉트 처리)
  - **`layout.tsx`**: 전체 페이지 공통 레이아웃
  - **`globals.css`**: 전역 공통 스타일
  - **`auth/page.tsx`**: 인증 관련 페이지 진입점
  - **`auth/login/page.tsx`**: 로그인 페이지
- **`frontend/components/`**: 재사용 가능한 UI 컴포넌트 모음
  - **`analysis-comparison.tsx`**: 모델별 분석 결과 비교 UI
  - **`analysis-detail.tsx`**: 분석 결과 상세 보기 UI
  - **`auth-screen.tsx`**: 로그인/회원가입 화면 레이아웃
  - **`brand-wordmark.tsx`**: 브랜드 로고 및 워드마크
  - **`clear-stale-service-worker.tsx`**: 오래된 서비스 워커 정리 스크립트
  - **`dashboard.tsx`**: 사용자 대시보드 UI
  - **`floatwatch-chat.tsx`**: 채팅 및 문의 UI
  - **`ocean-board-page.tsx`**: 게시판 페이지 UI
  - **`ocean-watchmark.tsx`**: UI 장식 요소
  - **`public-home.tsx`**: 공개 홈 화면
  - **`public-page-header.tsx`**: 공개 페이지 헤더
  - **`upload-dialog.tsx`**: 파일 업로드 대화상자 UI
  - **`workspace-home.tsx`**: 로그인 사용자용 홈 화면
  - **`workspace-sections.tsx`**: 워크스페이스 섹션 레이아웃
- **`frontend/lib/`**: 공통 유틸리티 및 API 통신
  - **`api.ts`**: 백엔드 API 호출 공통 함수 (`fetch` + `credentials: "include"` 처리)
  - **`types.ts`**: 프론트엔드에서 공통으로 사용하는 타입 정의

## 3. 📂 기타 문서 및 핵심 요약

- **`BACKUP_RESTORE.md`**: 백업 및 복원 기능 사용 방법 문서
- **`README.md`**: 프로젝트 개요 및 전체 실행 방법 안내 문서

## 💡 핵심 요약

- **`backend/`**: 서버 API, 데이터베이스, AI 분석 엔진(YOLO), 인증, 파일 저장 보안 처리 담당
- **`frontend/`**: 웹 UI 화면, 라우팅 페이지, 컴포넌트, 백엔드 API 호출 통신 담당
- **`backend/app/analysis_service.py`**: 영상 및 AI 모델 분석이 실제로 실행되는 핵심 엔진
- **`backend/app/main.py`**: FastAPI 서버의 전체 관문 (API 진입점)
- **`frontend/app/page.tsx`**: 초기 라우팅 및 공개 홈 진입점
